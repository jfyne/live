{
    "name": "web",
    "version": "1.0.0",
    "description": "",
    "main": "live.js",
    "scripts": {
        "test": "npx jest",
        "build": "npx tsc -noemit && npx esbuild --bundle src/auto.ts --outfile=dist/auto.js --minify --sourcemap"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/jest": "^26.0.15",
        "esbuild": "^0.7.20",
        "jest": "^26.6.1",
        "prettier": "^2.2.1",
        "ts-jest": "^26.4.3",
        "typescript": "^4.0.3"
    }
}
