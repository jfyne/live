{
    "name": "@jfyne/live",
    "version": "0.0.0",
    "description": "Go live handler sibling package",
    "main": "dist/live.js",
    "scripts": {
        "test": "npx jest",
        "build": "npx tsc -noemit && npx esbuild --bundle src/auto.ts --outfile=dist/auto.js --minify --sourcemap && npx tsc -noemit && npx esbuild --bundle src/live.ts --outfile=dist/live.js --minify --sourcemap"
    },
    "keywords": [],
    "author": "jfyne",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "https://github.com/jfyne/live"
    },
    "devDependencies": {
        "@types/jest": "^26.0.15",
        "esbuild": "^0.7.20",
        "jest": "^26.6.1",
        "prettier": "^2.2.1",
        "ts-jest": "^26.4.3",
        "typescript": "^4.0.3"
    }
}
